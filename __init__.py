import network
# import tkinter as tk

# app = tk.Tk()
# frame = tk.Frame()
# frame.config(width=100, height=100)
# frame.pack()
# lab1 = tk.Label(frame, text="X1")
# num1 = tk.Entry(frame)
# lab1.grid(row=0, column=0)
# num1.grid(row=1, column=0)

# lab2 = tk.Label(frame, text="X2")
# num2 = tk.Entry(frame)
# lab2.grid(row=0, column=1)
# num2.grid(row=1, column=1)

if __name__ == "__main__":
    network.learn()
